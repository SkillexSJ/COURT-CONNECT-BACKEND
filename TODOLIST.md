# 🏆 Sport Venue Booking - Production TODO List

This document tracks the remaining features and improvements needed to make this backend production-ready.

## 🔴 Phase 1: Core Financials (Highest Priority)
- [ ] **Payment Module (`src/modules/payment`)**
    - [ ] Integrate Stripe SDK.
    - [ ] Implement `createPaymentIntent` endpoint for bookings.
    - [ ] Set up Stripe Webhook handler to listen for `payment_intent.succeeded`.
    - [ ] Implement Refund logic for cancelled bookings.
- [ ] **Coupon Module (`src/modules/coupon`)**
    - [ ] Create CRUD for Coupons (Admin only).
    - [ ] Implement `validateCoupon` logic (expiry, usage limit, min order amount).
    - [ ] Implement `CouponRedemption` tracking on successful payments.

## 🟠 Phase 2: Security & Data Integrity
- [ ] **Request Validation (Zod)**
    - [ ] Install Zod: `npm install zod`.
    - [ ] Create validation schemas for all POST/PATCH routes (e.g., `court.schema.ts`, `booking.schema.ts`).
    - [ ] Implement a global `validateRequest` middleware.
- [ ] **Audit Logging**
    - [ ] Create an `AuditLogService`.
    - [ ] Hook into sensitive actions (Role changes, Price updates, Deletions) to populate the `AuditLog` table.
- [ ] **Rate Limiting**
    - [ ] Implement `express-rate-limit` to prevent brute-force on auth and booking endpoints.

## 🟡 Phase 3: Media & Communication
- [ ] **Cloudinary Media Uploads**
    - [ ] Implement a `MediaService` using `multer` and `cloudinary`.
    - [ ] Allow Organizers to upload multiple images for a `Court`.
    - [ ] Implement primary image selection for court thumbnails.
- [ ] **Email Notifications (Nodemailer)**
    - [ ] Create email templates (HTML) for:
        - [ ] Booking Received (User).
        - [ ] New Booking Alert (Organizer).
        - [ ] Booking Approved/Rejected (User).
        - [ ] Payment Confirmation (User).

## 🟢 Phase 4: Advanced Features & UX
- [ ] **Availability Search**
    - [ ] Create a "Find Available" endpoint: `GET /api/courts/search?date=YYYY-MM-DD&time=18:00`.
    - [ ] Implement logic to filter out courts that are already booked or closed during that specific window.
- [ ] **Dashboard Analytics**
    - [ ] Expand `AdminService.getDashboardStats` to include revenue charts and monthly growth metrics.
    - [ ] Implement an Organizer-specific dashboard for court performance.
- [ ] **Booking Rules**
    - [ ] Implement "Lead Time": Prevent bookings that start in less than 2 hours.
    - [ ] Implement "Cancellation Window": Only allow refunds if cancelled 24h before the start time.

## ✅ Completed Tasks
- [x] Prisma Schema Design & Relationships.
- [x] Basic CRUD for Users, Courts, Bookings, and Announcements.
- [x] Advanced `QueryBuilder` for pagination/search/filtering.
- [x] Soft-delete implementation for Courts.
- [x] TypeScript Type Resolution (100% Type Safe).
- [x] ESM Configuration and Directory Structure.

---
*Generated by Gemini CLI - March 19, 2026*
